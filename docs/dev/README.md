# Developer documentation

## Installing

To install the package into your system, run the following command from the root of the repository.

```bash
pip install -e .
```

## Testing

To execute the tests, run the following commands from the root of the repository.

```bash
pip install -e ".[dev]"
pytest
```
